box: node
build:
    steps:
        - npm-install
       #- npm-test

deploy:
    steps:
        - script:
            name: npm config
            code: >
                npm config set loglevel warn;
                npm config set //registry.npmjs.org/:_authToken=$NPM_TOKEN -q
        - script:
            name: npm publish
            code: npm publish
